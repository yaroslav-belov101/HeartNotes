import type { Note, EventItem } from './types'

export const notes: Note[] = [
]

export const events: EventItem[] = [
]
// Start with empty events — user data is stored in app state / localStorage

export default {}
